import React from 'react';
import {
  View,
} from 'react-native';

export const Line = () => {
  return (
    <View
      style={{
        backgroundColor: '#e0e5e3',
        borderBottomColor: '#e0e5e3',
        borderBottomWidth: 1,
        height: 0,
        width: '100%',
        marginVertical: 0,
      }}
    />
  );
};